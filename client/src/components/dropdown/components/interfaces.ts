export interface TriggerProps {
  title: string;
  active: boolean;
  size?: "s" | "m" | "l" | "xl";
}

export interface ButtonParams {
  h: string;
  padding: string;
  fontsize: string;
  radius: string;
  gap: string;
  iconsize: string;
  iconPadding: string;
}
